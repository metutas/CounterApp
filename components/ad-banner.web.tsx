// Web derlemesi için: react-native-google-mobile-ads native-only olduğundan
// (web SDK'sı yok) burada hiç import edilmiyor, bileşen sadece hiçbir şey render etmiyor.
export function AdBanner() {
  return null;
}
